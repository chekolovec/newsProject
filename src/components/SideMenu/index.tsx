import React from "react";
import { ScrollView, Text } from "react-native";

export default () => {
  return (
    <ScrollView style={{ flex: 1 }}>
      <Text style={{ fontSize: 20, color: "red" }}>123</Text>
    </ScrollView>
  );
};
